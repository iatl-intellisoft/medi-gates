<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

    
     <record id="view_move_form_inherit" model="ir.ui.view">
            <field name="name">account.move.form</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='partner_id']" position="after">
                    <field name="hr_receipt" invisible="1"/>
                </xpath>
            </field>
        </record>

        <!-- Shortcuts -->
        <record id="act_account_move_hr_receipt" model="ir.actions.act_window">
            <field name="name">HR Receipt</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">account.move</field>
            <field name="view_mode">tree,form</field>
            <field name="domain">[('hr_receipt','=',True)]</field>
        </record>
        
	<menuitem id="menu_account_move_hr_receipt" action="act_account_move_hr_receipt" parent="account.menu_finance_entries"/>

    </data>
</odoo>
